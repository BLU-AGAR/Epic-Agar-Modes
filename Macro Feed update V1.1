// ==UserScript==
// @name         EpicAgarMods V1.1
// @namespace    http://tampermonkey.net/
// @version      1.1
// @description  Awsome Mod for agar.io!
// @author       Way Way
// @match        http://agar.io/
// @require      http://pastebin.com/raw/QrMULMQV
// @run-at       document-start
// @grant        none
// ==/UserScript==


window.onload = function() {
    //Q- Auto Feed
    console.log('called');
var interval;
var switchy = false;
$(document).on('keydown',function(e){
console.log('keydown e.keyCode="'+e.keyCode+'"');
if(e.keyCode == 81){
console.log('keydown 81, switchy '+switchy);
if(switchy){
return;
}
switchy = true;
interval = setInterval(function() {
console.log('firing');
$("body").trigger($.Event("keydown", { keyCode: 87}));
$("body").trigger($.Event("keyup", { keyCode: 87}));
}, 3);//increase this number to make it fire them out slower
}
})

$(document).on('keyup',function(e){
console.log('keyup e.keyCode="'+e.keyCode+'"');
if(e.keyCode == 81){
console.log('stop firing');
switchy = false;
clearInterval(interval);
return;b
}
})
// end of auto feed

    //Text Replace
    $("h2").replaceWith("<h2>Epic Agar Modes</h2>");
    
    //Zoom
    window.agar.minScale = -30;
    
    //Grid
    window.agar.drawGrid = false;
    
    //Show mass
    setShowMass(true);
    
    //Dark Mode
    setDarkTheme(true);
    
    //Acid Mode Part(Copy Code and paste below!): setAcid(true);
    
}
